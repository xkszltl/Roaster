FROM docker.codingcafe.org/sandbox/centos:stage-util

COPY [".", "/etc/codingcafe/scripts"]

RUN cp -f /etc/hosts /tmp && echo 10.0.0.10 {git,proxy,repo}.codingcafe.org > /etc/hosts && /etc/codingcafe/scripts/setup.sh pybind opencv leveldb rocksdb lmdb && cat /tmp/hosts > /etc/hosts && rm -f /tmp/hosts
