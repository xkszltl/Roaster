FROM centos/systemd

ADD ["setup.sh", "/etc/codingcafe/"]

RUN cp -f /etc/hosts /tmp && echo 10.0.0.10 {git,proxy,repo}.codingcafe.org > /etc/hosts && /etc/codingcafe/setup.sh pkg auth cuda slurm ompi nagios && cat /tmp/hosts > /etc/hosts && rm -f /tmp/hosts
