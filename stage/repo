FROM docker.codingcafe.org/sandbox/centos:stage-init

WORKDIR /etc/codingcafe
COPY [".", "."]
COPY ["cache.repo", "/etc/yum.repos.d/"]

RUN cp -f /etc/hosts /tmp && echo 10.0.0.10 {git,proxy,repo}.codingcafe.org > /etc/hosts && ./setup.sh repo && cat /tmp/hosts > /etc/hosts && rm -f /tmp/hosts
